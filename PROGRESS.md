# 進捗状況

## 現在のステップ

Step 2 — 文字列・スライス・位置追跡（進行中）

## 完了済みステップ

### フェーズ1: ミニ言語のコアを作る

- [x] Step 1: 基本型・変数・関数
- [ ] Step 2: 文字列・スライス・位置追跡
- [ ] Step 3: TokenKind と Token を定義する
- [ ] Step 4: Vec でトークン列を持つ
- [ ] Step 5: struct と impl でパーサを作る
- [ ] Step 6: AST を作る
- [ ] Step 7: 参照・借用・可変参照
- [ ] Step 8: Result / Option / ? とカスタムエラー型
- [ ] Step 9: 評価器を作る + Value enum + Display
- [ ] Step 10: HashMap で変数環境を持つ
- [ ] Step 11: 制御構文を追加する
- [ ] Step 12: print 文を追加する
- [ ] Step 13: モジュール分割
- [ ] Step 14: テスト体系化
- [ ] Step 15: REPL とファイル実行

### フェーズ2: 実用CLIを作る

- [ ] Step 1: CLI引数を受け取る
- [ ] Step 2: ファイルを読み込む
- [ ] Step 3: 実データに対して言語を使う
- [ ] Step 4: エラー設計を改善する
- [ ] Step 5: テストとリファクタリング

## 前回のセッション終了時のメモ

- Step 1 完了、Step 1 小テスト全問正解
- Step 2 進行中（途中まで）
- 学んだこと:
  - `String`（自分のノート、所有している、書き換えられる）と `&str`（指差し、借りている、読むだけ）
  - `&` で String を &str に変換できる（指差しにする）
  - 関数の引数を `&str` で受け取る理由 → 読むだけなら所有権を渡す必要がない
  - `&` で渡すと所有権が移らないので、同じ変数を何回でも使える
  - `.len()` や `.chars()` のようにドット（`.`）で値が持つ機能を呼び出せる
  - 関数を呼ぶたびに、その関数の中身が最初から実行される
  - Rust の実行開始地点は `fn main()`（Python と同じ流れ、スタート地点が明示的なだけ）

## 次回やるべき最小タスク

1. 文字列を1文字ずつ読む（`for c in input.chars()`）を実際に動かす
2. 位置追跡（Position）の考え方を学ぶ
3. Step 2 の残りを完了させる
