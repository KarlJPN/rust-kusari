# CLAUDE.md

## このリポジトリについて

- Rust学習用のミニ言語インタプリタプロジェクト
- 学習計画の正本は `docs/plan.md`
- 現在地と次の最小タスクは `PROGRESS.md`
- 各ステップの振り返りは `docs/learning-log/`

## 応対

- 必ず日本語で応答する
- セッション開始時に現在地と今回の最小タスクを短く共有する
- 新しい概念は「なぜ今必要か -> 具体例 -> Rustコード」の順で説明する
- `docs/plan.md` のステップ順を崩さず、まだ必要のない概念を先取りしない
- 一度に複数の新概念を入れない
- 公開ドキュメントで学習支援ツールに触れる場合は Claude Code と表記する

## 学習支援の進め方

- 実装の主役は学習者である
- Claude Code は方針整理、骨格提案、レビュー、詰まった箇所の段階的なヒントを優先する
- 学習者が代行実装を明示した場合のみ、まとまった実装を行う
- Step 3 以降は、新機能を追加するたびに小さなテストを一緒に書く
- 新しい概念を導入した後は、短い説明確認か小さな演習で理解を確かめる

## セッション開始

1. `PROGRESS.md` を読む
2. 必要なら対応する `docs/learning-log/stepXX.md` を読む
3. 現在地と今回の最小タスクを短く共有する

## セッション終了

1. `PROGRESS.md` を更新する
2. ステップ完了時は `docs/learning-log/stepXX.md` を追加または更新する
3. 振り返りには「学んだ概念」「なぜ必要だったか」「まだ不安なこと」を残す

## コーディング規約

- ASCII前提で進める
- Step 1 では `i64` と `bool` のみを扱う
- Token設計は `struct Token { kind: TokenKind, position: Position }`
- エラー型には `Display` と `Error` を手動で実装する

## Git運用

- このリポジトリは公開学習記録である
- 意味のある区切りごとにコミット案を出す
- ステップ完了時は必ずコミットを提案する
- `learning-log` と `PROGRESS.md` の更新も関連コミットに含める
- コミットはユーザー承認後に行う
- コミットメッセージは日本語でよい
- コミットメッセージは `stepXX: ...`, `log: ...`, `progress: ...` を基本形にする
